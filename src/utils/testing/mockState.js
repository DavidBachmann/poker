import initialState from '../../redux/initialState'

export default function mockState(
  players = 6,
  handsIn = 0,
  street = 0,
  betLevel = 1,
  extendInitialState = false,
) {
  let state

  return state
}
